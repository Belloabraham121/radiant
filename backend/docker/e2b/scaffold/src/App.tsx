/**
 * Placeholder — Radiant agent replaces this file via generate_app.
 * Keep exports default; agent edits should preserve a valid React tree.
 */
export default function App() {
  return (
    <main className="mx-auto flex min-h-screen max-w-lg flex-col items-center justify-center gap-4 p-8">
      <div
        className="rounded-3xl border-2 border-[var(--hero-ink)] bg-white p-8 text-center shadow-[6px_6px_0_var(--hero-ink)]"
      >
        <p className="text-xs font-bold uppercase tracking-[0.2em] text-[var(--hero-violet)]">
          Radiant
        </p>
        <h1 className="mt-2 text-2xl font-extrabold tracking-tight">
          Your app will appear here
        </h1>
        <p className="mt-2 text-sm text-[var(--hero-ink)]/60">
          Ask your agent to build something — this scaffold is ready for code.
        </p>
      </div>
    </main>
  );
}
