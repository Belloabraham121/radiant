export function buildArtifactEditLines(): string[] {
  return [
    "CRITICAL — edit_app vs generate_app: Once an artifact exists in this session (you previously called generate_app), ALWAYS prefer edit_app for follow-up changes. The user does NOT need to say 'edit' — any request that modifies the existing artifact should use edit_app unless it requires rewriting most of the app. Examples that MUST use edit_app: 'make the background dark', 'change the title to X', 'use Inter font', 'make the button bigger', 'add a loading spinner', 'remove the footer', 'change the color scheme to blue', 'fix the padding', 'swap the order of these two sections', 'add a new field to the form', 'add from/until date pickers', 'rename the header', 'update the placeholder text', 'make quotes live on input', 'integrate this API'. These are ALL edit_app — the user is describing what they want changed, not asking you to rebuild. Use generate_app ONLY when: (1) no artifact exists yet (first build), or (2) the change is so sweeping that most files need rewriting (e.g. 'completely redesign this as a dashboard' or 'start over'). NEVER call generate_app to tweak an existing artifact — that wipes the user's UI. When in doubt, use edit_app.",
    'CRITICAL — edit_app surgical edits (default): edit_app preserves all existing layout, colors, structure, handlers, and wiring — change ONLY what the user asked for. Use targeted find-and-replace: { edits: [{ path: "app/page.tsx", old_string: "<exact snippet from tool result>", new_string: "<snippet with minimal change>" }] }. old_string must be COPIED EXACTLY from the file contents in your latest generate_app or edit_app tool result — same casing, whitespace, and quotes. Never guess or paraphrase. Include 3–10 surrounding lines in old_string so it appears exactly once. For \'add a field\', insert JSX/state beside the existing field block — do NOT rewrite the page or restyle unrelated sections. Apply multiple small edits in one call if needed. replace_file: true is LAST RESORT ONLY — use only after EDIT_STRING_NOT_FOUND when the file must be structurally rewritten (e.g. removing an entire manual quote-button flow). NEVER use replace_file for small requests like adding inputs, changing text, or tweaking styles. IMPORTANT — auto-retry on EDIT_STRING_NOT_FOUND: If edit_app returns EDIT_STRING_NOT_FOUND, the error includes the full file content. You MUST immediately retry the edit by copying the EXACT text from the error\'s file content as old_string — never tell the user the edit failed. Silently retry until it works. Only report failure after 2 consecutive retries fail. IMPORTANT — where to apply edits: In Next.js apps, styles and structure live in MULTIPLE places — globals.css, Tailwind classes in JSX or TSX, inline styles, and component props. Before editing, scan the latest tool result to find WHERE the thing the user wants changed is actually defined. Rules: (1) Backgrounds/colors: check for Tailwind bg-[...]/text-[...]/border-[...] classes on JSX elements FIRST — a div with min-h-screen bg-[#0b1020] visually overrides any body CSS. Edit the className, not just globals.css. (2) Fonts: check both globals.css (font-family) AND className (font-sans, font-mono, font-[...]) on the root container/body. (3) Spacing/padding/margins: almost always Tailwind classes (p-4, mx-auto, gap-6) on the JSX element — edit className. (4) Layout changes (reorder sections, add/remove components): edit the JSX structure in app/page.tsx or the relevant component file. (5) Adding new components: create the component file AND add the import + JSX usage in the parent. (6) Text/labels/titles: find the literal string in the JSX and edit it. (7) Borders/shadows/rounded corners: Tailwind classes (border, shadow-lg, rounded-xl) on the element. (8) Responsive behavior: Tailwind responsive prefixes (sm:, md:, lg:) on the element. General principle: if a Tailwind class controls the style, edit the className — CSS-only edits will be invisible. If the style is only in CSS (e.g. custom animations, @keyframes), edit the CSS file. When in doubt, edit BOTH the CSS and the JSX className to ensure the change is visible. Always include edits for ALL files where the target style appears.',
  ];
}
